import { CalendarDays } from 'lucide-react'
import { useMemo, useState } from 'react'
import { useFinance } from '../contexts/FinanceContext'
import { brl, priorityClass } from '../utils/format'
export default function CalendarPage(){
 const {accounts}=useFinance(); const [cursor,setCursor]=useState(()=>new Date()); const year=cursor.getFullYear(),month=cursor.getMonth(); const first=new Date(year,month,1); const start=(first.getDay()+6)%7; const days=new Date(year,month+1,0).getDate(); const cells=Array.from({length:start+days},(_,i)=>i<start?null:i-start+1); const byDay=useMemo(()=>{const map=new Map<number,typeof accounts>();accounts.filter(a=>{const d=new Date(`${a.dueDate}T12:00:00`);return d.getFullYear()===year&&d.getMonth()===month}).forEach(a=>{const day=new Date(`${a.dueDate}T12:00:00`).getDate();map.set(day,[...(map.get(day)||[]),a])});return map},[accounts,year,month]);
 return <div className="page-stack"><div className="page-title"><div><span className="eyebrow">Agenda</span><h1>Calendário financeiro</h1><p>Visualize vencimentos e recebimentos no mês.</p></div></div><div className="panel calendar-panel"><div className="calendar-nav"><button className="secondary-btn" onClick={()=>setCursor(new Date(year,month-1,1))}>←</button><h2>{new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(cursor)}</h2><button className="secondary-btn" onClick={()=>setCursor(new Date(year,month+1,1))}>→</button></div><div className="calendar-head">{['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'].map(d=><span key={d}>{d}</span>)}</div><div className="calendar-grid">{cells.map((day,i)=><div className={`calendar-cell ${!day?'muted-cell':''}`} key={i}>{day&&<><b>{day}</b><div>{(byDay.get(day)||[]).slice(0,3).map(a=><span key={a.id} className={`calendar-event ${a.kind==='receber'?'income':priorityClass(a.dueDate)}`}><CalendarDays size={12}/>{a.name} · {brl(a.amount)}</span>)}</div></>}</div>)}</div></div></div>
}
